# OS / editor
.DS_Store
Thumbs.db
*.log
.vscode/
.idea/

# Node (not required at runtime, but in case you add tooling)
node_modules/
npm-debug.log*

# Never commit secrets
wp-config.php
*.env
.env
admin-portal/config.php

# FTP dump / live site copy (never track WP core files)
enternstech/

# WinSCP credentials (fill in ftp-credentials.txt locally, never commit)
ftp-credentials.txt

# Claude Code IDE settings
.claude/

# Local archives
*.zip
